# システム概要

```json
{
  "sections": [
    {
      "title": "システム構成概要",
      "content": "### 変更サマリー\n| 区分 | 件数 | 主な内容 |\n|------|------|----------|\n| 追加 | 0件 | - |\n| 修正 | 3件 | mDNSパケットループ防止ガードの強化、プロセス排他制御の強化、ログ圧縮処理の非同期化・最適化 |\n| 拡張 | 0件 | - |\n| 削除 | 0件 | - |\n\n## 既存（参考）\nmDNS Proxyは、mDNSリクエストの処理・中継・名前解決を行うプロキシデーモンおよびCLIユーティリティです。\n主な構成要素は以下の通りです：\n- APIサーバー (HTTPによるレコード同期・情報提供)\n- mDNSサーバー (UDP 5353によるクエリ送受信)\n- スケジューラ (常駐バッチタスク: クリーンアップ、名前解決、発見、同期、マージ)\n- CLI (コンソールベースの対話型管理機能)\n- データベース (SQLiteによるホストおよびレコードの永続化)\n\n## 修正\n- **パケット処理ロジック:** 変更前は自己再帰的なパケットの処理（無限ループ）のガードが不十分でしたが、変更後はQRビットの判定および送信元IPアドレスの自己判定（自ノードおよびループバックアドレス）によるパケットの早期破棄処理を追加し、自己参照ループを完全に防止します。\n- **排他制御処理:** 変更前はPIDファイルの単純な存在確認のみでしたが、変更後はクラッシュ後の再起動時において、各OS固有API（Windowsはctypes、Linuxはos.kill）を用いた確実なプロセス生存確認処理を実装します。\n- **ログローテーション処理:** 変更前は日次ロールオーバー時にZIP圧縮を同期的に実行していましたが、変更後はログ出力に対する遅延影響を最小化する処理フロー（非同期化等）に最適化します。"
    },
    {
      "title": "使用技術スタック",
      "content": "### 変更サマリー\n| 区分 | 件数 | 主な内容 |\n|------|------|----------|\n| 追加 | 0件 | - |\n| 修正 | 1件 | SQLite接続タイムアウト設定と最適化オプションの明示化 |\n| 拡張 | 0件 | - |\n| 削除 | 0件 | - |\n\n## 既存（参考）\n- **動作環境:** 標準OS (Windows, Linux), MicroPython (Raspberry Pi Pico)\n- **言語:** Python 3.x / MicroPython\n- **データベース:** SQLite3 (WALモード有効)\n- **ネットワーク通信:** UDP (ポート 5353, mDNS), TCP (HTTP, RESTful API)\n\n## 修正\n- **SQLite接続設定:** \n  - 変更前: デフォルトの接続タイムアウト\n  - 変更後: 接続タイムアウトを明示的に30.0秒に設定し、`PRAGMA synchronous=NORMAL`の設定とWALモードの組み合わせによって、スケジューラやAPIからの同時アクセス時のDBロックエラー発生率を低減し耐ロック性を向上させます。"
    },
    {
      "title": "システム間連携",
      "content": "### 変更サマリー\n| 区分 | 件数 | 主な内容 |\n|------|------|----------|\n| 追加 | 0件 | - |\n| 修正 | 1件 | レコード同期APIにおけるシステム間連携の認証方式強化 |\n| 拡張 | 0件 | - |\n| 削除 | 0件 | - |\n\n## 既存（参考）\nネットワーク上に分散配置されたmDNS Proxyノード間で、互いにREST API (`/api/other-records`) を呼び出し、保有するレコード情報（`self_records`, `other_records`）を同期します。\nまた、DNS名前解決のため、マルチキャストDNSクエリやOSのpingコマンドを利用して他のネットワーク機器と連携します。\n\n## 修正\n- **プロキシ間連携API認証:**\n  - 変更前: 固定トークンのプレフィックスによる簡易判定によるシステム間連携。\n  - 変更後: `Authorization: Token` に加えて、HTTPヘッダーの `X-Sender-Node-ID` を組み合わせたプロキシ識別と認証に厳密化し、未承認ノードからの不正なレコード登録リクエストの遮断を確実に行います。"
    },
    {
      "title": "データフロー概要",
      "content": "### 変更サマリー\n| 区分 | 件数 | 主な内容 |\n|------|------|----------|\n| 追加 | 0件 | - |\n| 修正 | 2件 | パケット受信時の早期破棄フローの追加、マルチキャスト送信IFの明示指定によるパケット到達フローの改善 |\n| 拡張 | 0件 | - |\n| 削除 | 0件 | - |\n\n## 既存（参考）\n1. **名前解決フロー:** 定期的に `static_hosts` を読み込み、mDNSクエリおよびpingを実行して名前解決し、結果を `self_records` に登録します。\n2. **同期・マージフロー:** `self_records` と他ノードから受信した `other_records` を統合し、最終的な応答用レコードとして `merged_records` を生成します。\n3. **クエリ応答フロー:** UDP 5353ポートでmDNSクエリを受信時、`merged_records` を検索し合致するレコードがあれば応答パケットを返却します。\n\n## 修正\n- **クエリ受信フロー:**\n  - 変更前: 受信したパケットをそのまま解析・処理。\n  - 変更後: 受信直後にQRビット（応答パケットフラグ）を評価し、応答パケットまたは自ノード由来のクエリパケットを早期破棄するフィルタリングフローを追加し、無駄なパケット処理を抑止します。\n- **マルチキャスト送信フロー:**\n  - 変更前: OSのデフォルトルーティングポリシーに依存したパケット送信。\n  - 変更後: `IP_MULTICAST_IF` ソケットオプションを用いて、メインIPを持つネットワークインターフェースを明示的に指定してマルチキャスト送信を行うようフローを改善します。"
    },
    {
      "title": "セキュリティ概要",
      "content": "### 変更サマリー\n| 区分 | 件数 | 主な内容 |\n|------|------|----------|\n| 追加 | 0件 | - |\n| 修正 | 2件 | 不正ノードからのレコード登録防止策の強化、DoS的自己ループの防止策 |\n| 拡張 | 0件 | - |\n| 削除 | 0件 | - |\n\n## 既存（参考）\n- **ローカル管理:** SQLiteデータベースファイルはローカルに保存され、OSのファイルアクセス権限に依存します。\n- **API通信:** HTTPSは標準実装されておらず、プライベートネットワーク内での安全な運用を前提としています。\n\n## 修正\n- **APIのノード認証の厳密化:**\n  - 変更前: APIへの書き込みリクエストは簡易的なトークンのみで認可。\n  - 変更後: `X-Sender-Node-ID` とトークンを組み合わせることで、意図しない不正なノードやスプーフィングによるレコード書き込みを完全に遮断します。\n- **トラフィック制御（自己ループによるDoS的状態の防止）:**\n  - 変更前: ループガードが不完全で、ネットワーク帯域やCPUを圧迫する可能性がありました。\n  - 変更後: mDNSクエリ処理における自パケットおよび応答パケットの早期ドロップ機構により、意図しないトラフィック増大やプロセス負荷を確実に防ぎます。"
    },
    {
      "title": "インフラ構成概要",
      "content": "### 変更サマリー\n| 区分 | 件数 | 主な内容 |\n|------|------|----------|\n| 追加 | 0件 | - |\n| 修正 | 1件 | 複数ネットワークインターフェース対応の強化（mDNSパケットルーティング） |\n| 拡張 | 0件 | - |\n| 削除 | 0件 | - |\n\n## 既存（参考）\n- **ディレクトリ構成:** ルートディレクトリに `src/` (ソースコード), `db/` (データベースファイル), `log/` (ログファイル) を配置。\n- **プロセス構成:** 単一のメインプロセス内に、スケジューラスレッド、APIサーバースレッド、mDNSリスナースレッドを内包し、バックグラウンド動作を維持します。\n- **プラットフォーム互換性:** Raspberry Pi Pico (MicroPython), Windows PC, Linuxサーバーなど、多様な環境で単一コードベースにて動作します。\n\n## 修正\n- **ネットワークインターフェースバインディング:**\n  - 変更前: 仮想NICや複数の物理NIC（Wi-Fi + 有線LAN等）が存在する環境で、マルチキャストが意図しないNICから送出されるリスクがありました。\n  - 変更後: メインルートとなるIPアドレスを動的に検出し、マルチキャストパケットの送信元インターフェースとしてソケットレイヤ（`IP_MULTICAST_IF`）で明示的に指定するよう設定を修正します。これにより、複雑なネットワークインフラ環境下での稼働安定性が向上します。"
    }
  ]
}
```