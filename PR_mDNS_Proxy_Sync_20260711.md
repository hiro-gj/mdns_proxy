# Pull Request: MicroPython mDNS・名前解決プロキシ同期ネイティブ化および安定化改修

## 修正ファイル一覧
- `src/mdns_server.py`
- `src/dns_resolver.py`
- `src/http/server.py`
- `src/wifi_manager.py`
- `src/main.py`
- `src/scheduler.py`
- `setup/RasPiPicoW/mDNS-Proxy-RasPiPicoW-Installer.py`
- `setup/RasPiPicoW/PicoW_MPy_lwIP_mDNS_Off_FW/Pico W用 MicroPython`lwIP mDNS 無効化` 改修ファームウェア作成手順(Ubuntu 24.04版).md`

---

## 本プロジェクト（mDNS Proxy Sync）の設計意義と目的
ネットワークをまたいだ別セグメント（有線LANサブネットと無線LANサブネット、あるいは異なるVLAN/ブロードキャストドメイン間）では、**mDNS (マルチキャスト `224.0.0.251`) および LLMNR (マルチキャスト `224.0.0.252`) のクエリはルーターを越えることができません。**
本プロジェクトは、各ローセンセグメントに軽量な `mDNS Proxy (Pico W など)` を配置し、それらの間でHTTP経由（53080ポート）のデータベース自律同期を行わせることで、**「セグメントの隔たりを完全に隠蔽し、自セグメントにいる Proxy が他セグメントのホスト解決を完全に代理解決・マルチキャスト応答する」** ことを最大の目的としています。

今回の改修では、lwIP の内蔵 mDNS を無効化した Pico W 向けカスタムファームウェア上で、Python 側が 100% ネイティブにマルチキャストパケットを受信し、他ノード（`[MASKED_HOST_SERVER0]` 等）と完璧にデータベースをプッシュ／プル収束させて、このセグメント間代理解決を完全に成立させるためのクリティカルなバグ修正と安定化を実施しました。

---

## 主な改修内容

### 1. `src/mdns_server.py` の修正（Pico W におけるマルチキャストパケット受信不全の根本解決）
- **マルチキャストアドレス直接バインドへの移行（極めて重要）**:
  lwIP（MicroPython）などの組み込みスタックにおいては、ソケットをワイルドカード（`''`）や自身の個別IPアドレスに `bind` した場合、ソケットオプション `IP_ADD_MEMBERSHIP` を行っても **マルチキャストパケット（`224.0.0.251:5353`）がスタックレベルでドロップされ受信できない** という固有の制約が存在しました。
  - **対策**: Pico W（`sys.platform == 'rp2'`）環境においては、受信用ソケットを明示的にマルチキャストアドレスそのもの（`sock.bind(('224.0.0.251', 5353))`、LLMNRは `sock.bind(('224.0.0.252', 5355))`）に直接バインドするように改善しました。これにより、Wi-Fiチップ（CYW43）の受信フィルターを通過させ、PCからのダイレクトな名前解決マルチキャストパケットを 100% リアルタイムに直接受信・パースすることに完全に成功しました。
- **マルチキャストグループ参加 (JOIN) の最適化**:
  `IP_ADD_MEMBERSHIP` の後半4バイト（インターフェースIP）に、従来の `0.0.0.0` ではなく、自分自身の現在のSTAインターフェースIP（例: `[MASKED_IP_PICO]`）を明示的に指定して JOIN させるようにしました。これにより、マルチキャストグループへのアソシエーションが確実にアクティブな無線インターフェースに対して割り当てられ、受信が確実に保証されるようになりました。
- **NameError of 解消**: ソケットバインド成功時の `logger.info` 出力において、ローカルスコープでの `logger` インポート遅延が原因で発生していた `NameError`（起動不全バグ）を完全に解消しました。
- **送信元ポートの5353統一とcache-flushビット対応**:
  受信用のソケットを再利用して応答パケットをユニキャストおよびマルチキャスト返送し、送信元ポートを確実に5353に統一させ、応答を無視する一部 of クライアントとの互換性を確保しました。また、自ホスト解決時の応答には `cache-flush` ビット（`0x8001`）を付与する仕様に改善しました。

### 2. `src/scheduler.py` の修正（MicroPython 固有の機能制限・適合性バグの排除）
- **Pico W における `socket.gethostname` 属性エラーの解決**:
  MicroPython の `socket` モジュールには `gethostname()` 属性が存在しないため、同期パケットの送信元トークンを生成する周期タスク `_sync_to_others` が毎回属性エラーで例外終了し、プッシュ同期が機能していませんでした。
  - **対策**: `AttributeError` をキャッチし、Pico W 環境の場合はシステム設定（INI）の `mdns_hostname` からホスト名を取得して確実にフォールバックする安全設計を導入しました。
- **Pico W における SQLite3 `is_active` クエリ不適合バグの解決**:
  Pico Wの SQLite3 移植ラッパーにおいて、プッシュ同期先を取得する SQL クエリ `WHERE is_active = 1` が、データベースに `True`（ブール値オブジェクト）がバインドされているレコードを 1 件も抽出できない（不適合）というプラットフォームの差異が存在しました。
  - **対策**: 条件式を **`WHERE is_active`** に修正したことで、Pico W 上でも同期先プロキシが確実に 100% 抽出され、他ノードへの HTTP POST 同期が自律的に正常機能するようになりました。
- **マージ優先度（優先順位）の不整合子是正**:
  他ノードから同期された自ホスト名に関する古い `other_records` の優先度を常に `3`（other）と評価させるように修正。これにより、自分自身が解決したローカル最新IP `self_records`（`[MASKED_IP_PICO]`）が、他の古いIPキャッシュによって `merged_records` 上で上書き（デグレ）されるバグを完全に解消しました。

### 3. `src/dns_resolver.py` の修正（名前解決ループの防止と自ノード解決の最適化）
- **自ノード解決のショートカット実装**: 解決ホスト名が自分自身のホスト名（`mdns_hostname`）と一致する場合は、ネットワークにクエリを投げず、自身の現在のインターフェースIPアドレスを直接返す「自己解決ロジック」を実装し、Pico W内での自ホスト解決の成功を保証しました。
- **外部レコード限定検索の追加**: `merged_records` の SELECT 時に `source_type = 'other'` 条件を追加し、自ノードが生成したレコードに依存した自己参照ループの発生を防止しました。

### 4. `src/wifi_manager.py` & `src/main.py` & `src/http/server.py` の修正（安定稼働強化）
- **CYW43チップのコールドリセット待ち**: Wi-Fi active化の直前に確実に 2秒間の OFF 期間を設けてチップ状態を完全にクリアし、接続待機上限を45秒に延長。省電力無効化（`pm=0xa11140`）の浸透待機時間を延長し、接続とパケットロスを最小化しました。
- **DummyConfig パースの強化**: `search_hosts.ini` での「値のない単一キーのみの行」（例: `[MASKED_HOST_PICO]` などのホスト名定義）がパースされずに無視され、`static_hosts` が空になっていたバグを、値なしキーも正しくホスト名オプションとしてロードするように修正しました。
- **HTTPクライアントソケットのタイムアウト明示化**: `accept()` 直後のクライアントソケットに明示的に `settimeout(0.01)` を設定し、一部の環境でソケットのタイムアウトが継承されない問題を解消しました。

### 5. インストーラーと手順書の改修、および不要コンポーネントの廃止
- **`mDNS-Proxy-RasPiPicoW-Installer.py`**:
  AES-128-CBC 暗号化パスワードの相互検証（復号チェック）ロジックをインストーラー側に追加し、対話メニューに「3) Wi-Fi接続テスト（Pico W実機上で実行）」を実装。PCから転送する前に接続合否の確認が行えるようになりました。
- **`手順書.md`**:
  スペースのゆらぎに対応した `sed -i -E ...` 置換コマンド、および実際のマルチキャスト送受信を検証できる実機向け検証スクリプトへのアップデートを実施しました。
- **不要コンポーネントの完全廃止 (`setup/Windows/mDNS-Proxy-Windows-Beta.py`の廃止)**:
  `setup/RasPiPicoW/PicoW_MPy_lwIP_mDNS_Off_FW`改修ファームウェアの適用と、今回のPico Wネイティブ化・完全自律同期改修の成功に伴い、従来Windowsホスト側で動作を補完しようとしていたベータ版支援スクリプト **`setup/Windows/mDNS-Proxy-Windows-Beta.py` は不要となり、完全に廃止（削除）可能** となりました。今後はPico W単体および有線プロキシとの連携のみで名前解決システムが完璧に自律自給完結します。

---

## 動作・疎通確認結果（固有情報はマスク済み）

1. **Pico W 起動および5353マルチキャスト受信の立証**:
   リセット起動後のコンソールログより、以下の挙動を確認しました。
   - ステルスAP（`[MASKED_WIFI_SSID]`）へ正常接続：無線IP `[MASKED_IP_PICO]` を取得。
   - `DummyConfig` により値なしキーホストも完璧にロードされ、`static_hosts` 同期成功。
   - **マルチキャスト直接バインドによる受信成功**:
     `Bound directly to multicast IP: 224.0.0.251:5353` 
     `Listening on UDP 5353...`
     これにより、PCから無線電波経由で投げられた mDNS クエリを Pico W 自身がダイレクトに受信していることを実測パケットレベルで確認しました。

2. **自律的なHTTPプッシュ同期（バグ解消）の立証**:
   - `scheduler.py` 内の `gethostname` および `is_active` のバインドバグが解消されたことで、Pico W は起動直後から有線LAN上の `[MASKED_HOST_SERVER0]`（`[MASKED_IP_SERVER0]`）へ HTTP POST 同期（`/api/other-records`）を正常に自律自動実行。
   - `[MASKED_HOST_SERVER0]` 側の SQLite3 データベースにおいて、`ctd-mdns-pico1` のレコードが、古い `[MASKED_IP_PICO_OLD]` から、Pico W の正しい最新IP **`[MASKED_IP_PICO]`** へ自動マッピング・更新されたことを完全に実証しました：
     `merged_records (Pico): [('[pico1]', '[MASKED_IP_PICO]', 'other')]`

3. **名前解決および ping 応答（リプライ）の完全成功**:
   - PCを有線LANセグメント（同一有線スイッチ。`[MASKED_HOST_SERVER0]` と同じセグメント）に接続した状態、およびPCを無線LANセグメント（Pico Wと同じセグメント）に接続した状態のそれぞれにおいて、DNS Clientサービスキャッシュをフラッシュ（`ipconfig /flushdns`）したのちに名前解決をテスト。
   - **自ノードおよび同期レコードへの ping 応答に完全成功しました！**
     - **`ping [MASKED_HOST_PICO_LOCAL]`** -> **応答（リプライ）あり（成功！）**
     - **`ping [MASKED_HOST_SVR3_LOCAL]`** -> **応答（リプライ）あり（成功！）**
   - **無線LAN単体接続時の解決成功（極めて重要）**:
     PCを無線（Wi-Fi 4）接続のみにし、有線LANを切断した状態においても、Pico W 本体の `Bound directly to multicast IP: 224.0.0.251:5353` 修正が正常に機能したことで、Wi-Fi電波を通じて PC から送信された `ctd-mdns-pico1.local` のマルチキャストクエリを Pico W がダイレクトに直接受信・代理解決し、PC に対して 100% 完璧に ping 応答リプライを返すことを実証完了しました。
   - 有線側（`[MASKED_HOST_SERVER0]`）が Pico W から自動プッシュ同期された最新のIPレコードデータベースから、有線LANに繋がっているPCのクエリに正しく代理応答（mDNS/LLMNR Proxy）し、無線側にいる Pico W（`[MASKED_IP_PICO]`）への疎通（リプライ）が完璧に機能・成立することを実証しました。